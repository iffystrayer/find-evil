Captured tool outputs for MockExecutor. Filename is `<tool>.txt`. Replace these
with real outputs captured from the SIFT VM run (Day 4) to harden the regression
suite. `disk.img` is a tiny placeholder artifact for evidence registration tests.
